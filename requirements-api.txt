# Knowledge Lake API - Production Dependencies
flask==3.0.0
flask-cors==4.0.0
waitress==3.0.0
python-dotenv==1.0.0
requests==2.31.0

# mem0 core dependencies (will use existing installation)
# mem0==0.1.0  # Using local development version
